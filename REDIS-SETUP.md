# Настройка Redis для кеширования

## Что было сделано

1. ✅ Добавлен Redis сервис в `docker-compose.yml`
2. ✅ Установлено PHP расширение Redis в `Dockerfile`
3. ✅ Обновлена конфигурация CakePHP для использования Redis вместо File cache

## Как применить изменения

### 1. Пересобрать образ PHP

```bash
docker compose build tyre-app-php
```

Это установит PHP расширение Redis (версия 2.2.8 для совместимости с PHP 5.6).

### 2. Перезапустить контейнеры

```bash
docker compose down
docker compose up -d
```

Это запустит Redis контейнер и перезапустит PHP контейнер с новым расширением.

### 3. Проверить работу Redis

```bash
./check-redis.sh
```

Скрипт проверит:
- Работает ли Redis сервис
- Установлено ли PHP расширение Redis
- Правильно ли настроена конфигурация CakePHP
- Работает ли запись/чтение из кеша

## Конфигурация

### Redis сервис
- **Контейнер**: `tyre-app-redis`
- **Порт**: `6379` (можно изменить через `REDIS_PORT` в `.env`)
- **Память**: `256MB` (настраивается в `docker-compose.yml`)
- **Политика**: `allkeys-lru` (удаление наименее используемых ключей при нехватке памяти)

### CakePHP Cache
- **Движок**: Redis (автоматически выбирается, если расширение доступно)
- **Сервер**: `tyre-app-redis`
- **Порт**: `6379`
- **Persistent connection**: Да (для лучшей производительности)

## Приоритет выбора движка кеша

1. **Redis** (если расширение `redis` загружено) ← **Используется сейчас**
2. **APC** (если расширение `apc` загружено и Redis недоступен)
3. **File** (по умолчанию, если Redis и APC недоступны)

## Проверка текущего движка кеша

```bash
docker exec tyre-app-php php -r "
require '/var/www/html/app/webroot/index.php';
\$config = Cache::config('default');
echo 'Cache engine: ' . \$config['engine'] . PHP_EOL;
"
```

Должно показать: `Cache engine: Redis`

## Преимущества Redis

- ⚡ **Быстрее**: данные в оперативной памяти, а не на диске
- 📈 **Масштабируемость**: можно использовать несколько серверов Redis
- 🔄 **Персистентность**: данные сохраняются на диск (AOF)
- 💾 **Эффективное использование памяти**: автоматическое удаление старых ключей

## Устранение проблем

### Redis не запускается
```bash
docker compose logs tyre-app-redis
```

### PHP расширение Redis не установлено
```bash
docker exec tyre-app-php php -m | grep redis
# Должно показать: redis
```

### Ошибка подключения к Redis
Проверьте, что контейнеры в одной сети:
```bash
docker network inspect tyre-php-app_tyre-app-network
```

### Проверка подключения из PHP контейнера
```bash
docker exec tyre-app-php ping -c 2 tyre-app-redis
```

## Мониторинг Redis

```bash
# Статистика Redis
docker exec tyre-app-redis redis-cli INFO stats

# Количество ключей
docker exec tyre-app-redis redis-cli DBSIZE

# Использование памяти
docker exec tyre-app-redis redis-cli INFO memory
```
